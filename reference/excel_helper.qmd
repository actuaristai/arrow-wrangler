# excel_helper { #arrow_wrangler.excel_helper }

`excel_helper`

Excel export with metadata.

## Functions

| Name | Description |
| --- | --- |
| [export_to_excel](#arrow_wrangler.excel_helper.export_to_excel) | Use xlwings to export and format data with timestamp. |

### export_to_excel { #arrow_wrangler.excel_helper.export_to_excel }

```python
excel_helper.export_to_excel(df, file_name, sheet_name)
```

Use xlwings to export and format data with timestamp.

Args:
    df (pd.DataFrame): Data to export.
    file_name (str): Filename and path to export.
    sheet_name (str): Sheet name, also becomes table name.